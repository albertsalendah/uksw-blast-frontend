name: deploy
on: [push]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3 
      - name: copy web build
        run: cp -r ./build/web /var/www/uksw-blast-frontend-build
